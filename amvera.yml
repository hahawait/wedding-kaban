meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: backend/requirements.txt

run:
  command: cd backend && uvicorn main:app --host 0.0.0.0 --port 5000
  containerPort: 5000
