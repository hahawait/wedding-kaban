meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: backend/requirements.txt

run:
  command: uvicorn main:app --host 0.0.0.0 --port 5000 --app-dir backend
  containerPort: 5000
